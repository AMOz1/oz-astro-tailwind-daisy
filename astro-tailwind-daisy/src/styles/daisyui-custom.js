// DaisyUI custom theme configuration
export default {
  // Enable theme switching
  themes: true,
  // Define default themes
  defaultTheme: 'light',
  // Base component styles
  base: true,
  // Utility classes
  utils: true,
  // Responsive variants
  rtl: false,
  // Prefix for daisyUI classes
  prefix: '',
  // Enable dark mode
  darkTheme: 'dark',
  // Theme variables
  themeRoot: ':root',
};